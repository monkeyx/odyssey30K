class ItemsController < ApplicationController
  require_role 'zeus', :for_all_except => [:index, :show]
  require_role 'freeman', :for => [:index, :show]
  require_role 'zeus', :for => :show, :unless => "current_user.authorized_for_item?(params[:id])"
  
  # GET /items
  # GET /items.xml
  def index
    filter = params[:filter] || 'all'
    filter = 'all' unless Item::ALLOWED_FILTERS.include?(filter)
    finder = "Item.known_by_user_id(#{current_user.id}).#{current_user.item_restriction}.#{filter}"
    @items = eval(finder)

    respond_to do |format|
      format.html # index.html.erb
      format.xml  { render :xml => @items }
    end
  end

  # GET /items/1
  # GET /items/1.xml
  def show
    @item = Item.find(params[:id])

    respond_to do |format|
      format.html {render :layout => 'colorbox'} # show.html.erb
      format.xml  { render :xml => @item }
    end
  end

  # GET /items/new
  # GET /items/new.xml
  def new
    @item = Item.new
    @knowledges = Knowledge.find(:all)
    respond_to do |format|
      format.html # new.html.erb
      format.xml  { render :xml => @item }
    end
  end

  # GET /items/1/edit
  def edit
    @item = Item.find(params[:id])
    @knowledges = Knowledge.find(:all)
  end

  # POST /items
  # POST /items.xml
  def create
    @item = Item.new(params[:item])

    respond_to do |format|
      if @item.save
        format.html { redirect_to(@item, :notice => 'Item was successfully created.') }
        format.xml  { render :xml => @item, :status => :created, :location => @item }
      else
        @knowledges = Knowledge.find(:all)
        format.html { render :action => "new" }
        format.xml  { render :xml => @item.errors, :status => :unprocessable_entity }
      end
    end
  end

  # PUT /items/1
  # PUT /items/1.xml
  def update
    @item = Item.find(params[:id])

    respond_to do |format|
      if @item.update_attributes(params[:item])
        format.html { redirect_to(@item, :notice => 'Item was successfully updated.') }
        format.xml  { head :ok }
      else
        @knowledges = Knowledge.find(:all)
        format.html { render :action => "edit" }
        format.xml  { render :xml => @item.errors, :status => :unprocessable_entity }
      end
    end
  end

  # DELETE /items/1
  # DELETE /items/1.xml
  def destroy
    @item = Item.find(params[:id])
    @item.destroy

    respond_to do |format|
      format.html { redirect_to(items_url) }
      format.xml  { head :ok }
    end
  end
end
